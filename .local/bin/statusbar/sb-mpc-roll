#!/bin/bash

[[ "$(pgrep -c sb-mpc-roll)" -gt 1 ]] && {
    for pid in pgrep sb-mpc-roll ; do
        [[ "$pid" == "$$" ]] && continue
        kill "$pid"
    done
}

while : ; do
    kill -45 "$(pidof dwmblocks)"
    sleep 1
done
