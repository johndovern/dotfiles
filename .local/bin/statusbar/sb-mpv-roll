#!/bin/bash

while [[ "$(pgrep -c sb-mpv-roll)" -gt 1 ]] ; do
    kill $(pgrep -o sb-mpv-roll)
    # pgrep -o sb-mpv-roll
done

while :; do
    kill -56 $(pidof dwmblocks)
    echo test
    sleep 1
done
